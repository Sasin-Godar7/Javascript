// console.log("Hello World!");
// variable decleration in javascript
// we can decleare the variable using the let and var keyword
// example
var a = 10;
let b = 20;
// now days the var has become old and we usually use let keyword to decleare variables
// so for displaying the output on the screen can be done by using the console.log keyword
console.log("a="+a);
console.log("b="+b);
// now simply initializing the a string variable
let string = "prabin";
//simply displaying it 
console.log("My name is "+string); 
//for boolean datatypes we use
 let Boolean =true;
console.log(Boolean);
// if we are declearing a constant variable then we can simply use const keyword,this cannnot bbe changed further.
const age = 20;
//age = 30; This is wrong
// we can make object in javascripgt which makes a container where we can store multiple data of different datatype
// we generally decleare the object by using const keywird,example
const Student = {
     fullname:"prabin Thapa",
     age:20,
     address:chitwan,
     ispass:true
};
